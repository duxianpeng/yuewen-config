spring:
  application:
    name: yuewen-config
eureka:
  client:
    register-with-eureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: http://${HOST_NAME:localhost}:1111/yuewen-discover/eureka/